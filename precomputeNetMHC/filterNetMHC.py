import io
import argparse
import os

parser = argparse.ArgumentParser()
"""
Arguments:

Base (one, optional)
Additional FASTA files (zero or more)
Alleles (at least 1)
Lengths (at least 1)
Rank (Between 0 and 100)
"""
